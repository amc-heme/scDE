% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_dge.R
\name{run_dge}
\alias{run_dge}
\alias{run_dge.Seurat}
\alias{run_dge.SingleCellExperiment}
\alias{run_dge.AnnDataR6}
\title{Differential Gene Expression}
\usage{
run_dge(
  object,
  group_by,
  layer = NULL,
  seurat_assay = NULL,
  lfc_format = "log2",
  positive_only = FALSE,
  remove_raw_pval = FALSE,
  test_use = NULL,
  slot = lifecycle::deprecated(),
  ...
)

\method{run_dge}{Seurat}(
  object,
  group_by,
  layer = NULL,
  seurat_assay = NULL,
  lfc_format = "log2",
  positive_only = FALSE,
  remove_raw_pval = FALSE,
  test_use = NULL,
  slot = lifecycle::deprecated()
)

\method{run_dge}{SingleCellExperiment}(
  object,
  group_by,
  layer = NULL,
  seurat_assay = NULL,
  lfc_format = "log2",
  positive_only = FALSE,
  remove_raw_pval = FALSE,
  test_use = NULL,
  slot = lifecycle::deprecated()
)

\method{run_dge}{AnnDataR6}(
  object,
  group_by,
  layer = NULL,
  seurat_assay = NULL,
  lfc_format = "log2",
  positive_only = FALSE,
  remove_raw_pval = FALSE,
  test_use = NULL,
  slot = lifecycle::deprecated()
)
}
\arguments{
\item{object}{a single-cell object. Currently, Seurat,
SingleCellExperiment, and AnnData objects are supported.}

\item{group_by}{metadata variable to use for forming differential gene
expression groups. A group will be created for all values in this variable
present in the object passed to this function.}

\item{layer}{The feature matrix layer to pull data from. If NULL,
test-specific conventions will be used. The "data" layer is used for Seurat
objects, and the "logcounts" layer is used for SingleCellExperiment objects.
This parameter is currently ignored for anndata objects: only the "X" matrix
may be used. The layer parameter is also ignored in BPCells objects.}

\item{seurat_assay}{for Seurat objects, the assay to use for evaluating
differences in expression. This is used only for tests on Seurat objects.
For SingleCellExperiment objects, the assay defaults to the main experiment,
and for anndata objects the "X" matrix is used.}

\item{lfc_format}{sets the format used for reporting log-fold changes. If
"log2", log-fold changes will be reported in a log two fold change format,
and if "ln", log-fold changes will be reported as a natural log fold change.
"log2" is the default value. Presto uses a natural log fold change by
default, so \code{lfc_format} should be set to "ln" if consistency with Presto is
desired.}

\item{positive_only}{If TRUE, only display genes that are up-regulated in a
group compared to the reference (LFC > 0). Defaults to FALSE.}

\item{remove_raw_pval}{If TRUE, show only the adjusted p-value.
Defaults to FALSE.}

\item{test_use}{For SingleCellExperiment objects only. Controls which
backend is used. \code{NULL} (default) auto-detects: \code{"Presto"} for in-memory
matrices (\code{dgCMatrix} or \code{matrix}), \code{"scran"} for all other matrix classes
(e.g. HDF5-backed \code{DelayedMatrix}, \code{SVT_SparseMatrix}).
\code{"Presto"} forces presto via \code{presto::wilcoxauc} — errors if the matrix is
not \code{dgCMatrix} or \code{matrix}. \code{"scran"} forces a true one-vs-rest Wilcoxon
test per group via \code{scran::pairwiseWilcox}, indexing the result directly
(returns an \code{auc} column; LFC from group means). \code{"scran_pairwise"} uses
\code{scran::findMarkers} with pairwise comparisons combined across all groups
(\code{pval.type = "any"}); no \code{auc} column is returned and \code{log2FC} is computed
from group vs. all-other-cells means (same method as \code{"scran"}).}

\item{slot}{No longer used. Please use \code{layer} instead.}
}
\description{
Performs differential expression testing on single cell data depending on the
class of the object passed. See the "Methods (by class)" section for details
on the test used.
}
\section{Methods (by class)}{
\itemize{
\item \code{run_dge(Seurat)}: Seurat objects: uses Presto

\item \code{run_dge(SingleCellExperiment)}: SingleCellExperiment objects: uses Presto by default for
in-memory matrices, with automatic fallback to scran for DelayedArray-backed
matrices (e.g. HDF5-backed via \code{HDF5Array}). The backend can be controlled
explicitly via \code{test_use}.

\strong{Presto} (\code{test_use = "Presto"} or auto for in-memory): calls
\code{presto::wilcoxauc} directly on the extracted assay matrix. Output schema
matches the Seurat/Presto path and includes \code{auc}, \code{pct_in}, and \code{pct_out}.

\strong{scran one-vs-rest} (\code{test_use = "scran"} or auto for non-presto-compatible
matrices): runs a true one-vs-rest Wilcoxon test per group via
\code{scran::pairwiseWilcox} on a binary factor (group vs. all others), then
indexes the result directly by pair position. Returns an \code{auc} column. LFC
is computed from per-group means (log-space subtraction). Requires
\code{BiocManager::install(c("scran", "SummarizedExperiment"))}.

\strong{scran pairwise} (\code{test_use = "scran_pairwise"}): uses
\code{scran::findMarkers(pval.type = "any")}. No \code{auc} column. \code{log2FC} is
computed from group vs. all-other-cells means (same method as \code{"scran"}).
Requires \code{BiocManager::install(c("scran", "SummarizedExperiment"))}.

In all cases, \code{avgExpr} for scran paths is computed from the assay matrix
via \code{Matrix::rowMeans()} (scran does not output mean expression).

\item \code{run_dge(AnnDataR6)}: Anndata objects: uses Scanpy's rank_genes_groups
function, with the "wilcoxon" method

}}
